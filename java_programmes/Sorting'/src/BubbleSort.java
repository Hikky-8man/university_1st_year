public class BubbleSort {
    private int[] array;
    public BubbleSort(int[] array){
        this.array=array;
    }

    /*public int[] Sort(){
        int incraments=0;
        if(){

    }

    private boolean runThrough() {
        boolean flag=false;
        for (int item = 0; item >= this.array.length() - 1; item++){
            if (this.array[item]>this.array[item+1]){
                int temp=this.array[item+1];
                this.array[item+1]=this.array[item];
                this.array[item]=temp;
                flag=true;
            }
            return flag;
        }
    }
*/
}

